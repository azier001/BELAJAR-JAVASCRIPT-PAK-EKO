# 📚 JavaScript Synchronous & Asynchronous

## 🔄 Apa itu Synchronous?

**Synchronous** adalah cara eksekusi kode secara berurutan, di mana setiap baris kode harus menunggu baris sebelumnya selesai dijalankan.

### ✨ Karakteristik Synchronous:

- ⏱️ **Eksekusi Berurutan** - Program dalam JavaScript secara default akan dieksekusi baris per baris
- 🔒 **Blocking Process** - Secara default, proses di JavaScript akan dieksekusi secara Synchronous, artinya baris selanjutnya akan dieksekusi setelah baris sebelumnya selesai dikerjakan
- ⏸️ **Menunggu Selesai** - Proses Synchronous juga biasa disebut Blocking, karena harus menunggu tiap proses selesai, baru proses selanjutnya bisa dilakukan

### 💡 Contoh Synchronous:

```javascript
console.log("Mulai");

function hitungJumlah(a, b) {
  const hasil = a + b;
  return hasil;
}

const jumlah = hitungJumlah(5, 3);
console.log("Hasil penjumlahan:", jumlah);

console.log("Selesai");
```

**📝 Penjelasan Kode:**
- Pertama, program mencetak "Mulai"
- Kemudian memanggil fungsi `hitungJumlah()` dengan parameter 5 dan 3
- Program **menunggu** fungsi selesai menghitung sebelum melanjutkan ke baris berikutnya
- Setelah mendapat hasil, program mencetak hasil penjumlahan
- Terakhir, program mencetak "Selesai"

**🖥️ Output:**
```
Mulai
Hasil penjumlahan: 8
Selesai
```

---

## ⚡ Apa itu Asynchronous?

**Asynchronous** adalah cara eksekusi kode yang memungkinkan program melanjutkan eksekusi tanpa harus menunggu proses tertentu selesai.

### ✨ Karakteristik Asynchronous:

- 🚀 **Non-Blocking** - Walaupun secara default proses di JavaScript dieksekusi secara Synchronous, namun kita bisa membuatnya menjadi Asynchronous
- ⏩ **Tidak Menunggu** - Berbeda dengan proses Synchronous, pada proses Asynchronous, JavaScript tidak akan menunggu proses tersebut selesai, melainkan JavaScript akan melanjutkan baris selanjutnya, tanpa harus menunggu proses Asynchronous selesai
- 🔓 **Parallel Execution** - Proses Asynchronous juga biasa disebut Non-Blocking

### 💡 Contoh Asynchronous:

```javascript
console.log("Mulai");

setTimeout(function() {
  console.log("Proses Asynchronous selesai setelah 2 detik");
}, 2000);

console.log("Selesai");
```

**📝 Penjelasan Kode:**
- Pertama, program mencetak "Mulai"
- Kemudian `setTimeout()` dijadwalkan untuk dijalankan setelah 2 detik (2000ms)
- Program **TIDAK menunggu** setTimeout selesai, langsung melanjutkan ke baris berikutnya
- Program mencetak "Selesai" terlebih dahulu
- Setelah 2 detik berlalu, callback setTimeout baru dijalankan

**🖥️ Output:**
```
Mulai
Selesai
Proses Asynchronous selesai setelah 2 detik
```

> ⚠️ **Perhatikan:** Output "Selesai" muncul lebih dulu sebelum pesan dari setTimeout, meskipun setTimeout ditulis lebih awal dalam kode!

---

## 🔍 Perbandingan Synchronous vs Asynchronous

| Aspek | 🔄 Synchronous | ⚡ Asynchronous |
|-------|---------------|----------------|
| **Eksekusi** | Berurutan | Parallel/Non-blocking |
| **Blocking** | Ya (Blocking) | Tidak (Non-blocking) |
| **Menunggu** | Harus menunggu selesai | Tidak perlu menunggu |
| **Use Case** | Operasi sederhana, perhitungan | API calls, file reading, timers |

---

## 🎯 Kapan Menggunakan Asynchronous?

Gunakan **Asynchronous** untuk operasi yang membutuhkan waktu lama:

- 🌐 Request ke API/Server
- 📁 Membaca/Menulis file
- ⏰ Operasi dengan delay/timer
- 💾 Query ke database
- 🖼️ Loading gambar/resource

Dengan asynchronous, aplikasi tetap responsif dan tidak "freeze" saat menunggu operasi selesai! 🚀
