# 🚀 Async di JavaScript

## 📚 Apa itu Async?

**Async** adalah kata kunci khusus di JavaScript yang digunakan untuk menandakan bahwa sebuah function adalah **asynchronous** (tidak langsung selesai). Function yang diberi kata kunci `async` akan **otomatis mengembalikan Promise**.

### ✨ Keuntungan Menggunakan Async:

- ✅ Tidak perlu membuat Promise secara manual lagi
- ✅ Kode lebih ringkas dan mudah dibaca
- ✅ Otomatis membungkus return value menjadi Promise

---

## 🔍 Perbandingan: Dengan dan Tanpa Async

### 🟦 Cara Lama (Tanpa Async)

Dulu, kita harus membuat Promise secara manual seperti ini:

```javascript
// Promise<String>
const getNamePromise = () => {
  return new Promise((resolve, reject) => {
    resolve('Ahmad Promise');
  });
};

getNamePromise().then((name) => console.log(name));
```

**📝 Penjelasan Kode:**
- Kita membuat function `getNamePromise` yang mengembalikan Promise baru
- Di dalam Promise, kita harus memanggil `resolve()` untuk mengembalikan nilai
- Untuk mendapatkan hasilnya, kita gunakan `.then()`

**✅ Output:**
```
Ahmad Promise
```

---

### 🟩 Cara Baru (Dengan Async)

Dengan `async`, kode menjadi lebih sederhana:

```javascript
// Promise<String>
const getNameAsync = async () => {
  return 'Ahmad Async';
};

getNameAsync().then((name) => console.log(name));
```

**📝 Penjelasan Kode:**
- Cukup tambahkan kata kunci `async` sebelum `()`
- Langsung `return` nilai tanpa perlu membuat Promise manual
- JavaScript otomatis membungkus return value menjadi Promise
- Cara menggunakannya sama, pakai `.then()`

**✅ Output:**
```
Ahmad Async
```

---

## 💡 Contoh Sederhana 1: Mengambil Data User

```javascript
// Tanpa Async - Lebih Panjang
const getUserPromise = () => {
  return new Promise((resolve, reject) => {
    resolve({ name: 'Budi', age: 20 });
  });
};

getUserPromise().then((user) => {
  console.log('Nama:', user.name);
  console.log('Umur:', user.age);
});
```

**✅ Output:**
```
Nama: Budi
Umur: 20
```

```javascript
// Dengan Async - Lebih Ringkas
const getUserAsync = async () => {
  return { name: 'Budi', age: 20 };
};

getUserAsync().then((user) => {
  console.log('Nama:', user.name);
  console.log('Umur:', user.age);
});
```

**✅ Output:**
```
Nama: Budi
Umur: 20
```

**📝 Penjelasan:**
- Kedua kode menghasilkan output yang sama
- Versi `async` lebih pendek dan mudah dipahami
- Tidak perlu repot-repot membuat `new Promise()` lagi

---

## 💡 Contoh Sederhana 2: Menghitung Angka

```javascript
// Async function untuk perhitungan sederhana
const hitungKali = async (a, b) => {
  return a * b;
};

hitungKali(5, 3).then((hasil) => {
  console.log('Hasil perkalian:', hasil);
});
```

**📝 Penjelasan Kode:**
- Function `hitungKali` menerima 2 parameter: `a` dan `b`
- Mengembalikan hasil perkalian `a * b`
- Karena pakai `async`, hasilnya otomatis dibungkus dalam Promise
- Kita ambil hasilnya dengan `.then()`

**✅ Output:**
```
Hasil perkalian: 15
```

---

## 💡 Contoh Sederhana 3: Menyapa dengan Async

```javascript
// Function async untuk menyapa
const sapaPengguna = async (nama) => {
  return `Halo, ${nama}! Selamat datang di JavaScript 👋`;
};

sapaPengguna('Andi').then((salam) => {
  console.log(salam);
});
```

**📝 Penjelasan Kode:**
- Function `sapaPengguna` menerima parameter `nama`
- Mengembalikan string sapaan dengan template literal
- Hasilnya otomatis menjadi Promise karena ada kata kunci `async`

**✅ Output:**
```
Halo, Andi! Selamat datang di JavaScript 👋
```

---

## 🎯 Kesimpulan

| Tanpa Async | Dengan Async |
|-------------|--------------|
| ❌ Harus buat `new Promise()` | ✅ Cukup tambah kata kunci `async` |
| ❌ Harus panggil `resolve()` | ✅ Langsung `return` nilai |
| ❌ Kode lebih panjang | ✅ Kode lebih ringkas |

### 🔑 Poin Penting:
- `async` membuat function otomatis mengembalikan Promise
- Tidak perlu lagi membuat Promise secara manual
- Return value otomatis dibungkus dalam Promise
- Cara menggunakan hasilnya tetap sama dengan `.then()`

---

## 📖 Contoh Lengkap

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Async</title>
  </head>
  <body>
    <script>
      // Promise<String>
      const getNamePromise = () => {
        return new Promise((resolve, reject) => resolve('ahmad promise'));
      };

      getNamePromise().then((name) => console.log(name));

      // Promise<String>
      const getNameAsync = async () => 'ahmad async';

      getNameAsync().then((name) => console.log(name));
    </script>
  </body>
</html>
```

**✅ Output:**
```
ahmad promise
ahmad async
```

---

**🎓 Langkah Selanjutnya:** Setelah memahami `async`, kamu bisa lanjut belajar tentang `await` untuk membuat kode asynchronous yang lebih mudah dibaca!
