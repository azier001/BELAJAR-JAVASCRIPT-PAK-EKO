# 📡 Response Status di AJAX

## 🎯 Apa itu Response Status?

**Response Status** adalah kode angka yang dikirimkan server untuk memberitahu apakah permintaan kita berhasil atau gagal.

---

## 🔍 Mengapa Response Status Penting?

### 📌 Komunikasi Client dan Server

- AJAX adalah proses komunikasi antara **Client** (browser kita) dan **Server** (tempat data disimpan)
- Dalam komunikasi ini, kita **tidak bisa selalu menganggap** proses akan berjalan lancar
- Banyak hal yang bisa mengganggu proses AJAX dan menyebabkan error

### ⚠️ Hal-hal yang Bisa Menyebabkan Error:

- 🌐 **Koneksi internet bermasalah**
- 🖥️ **Error dari server**
- ❌ **Data dari client tidak valid**
- Dan lain-lain

### ✅ Solusinya?

Server akan mengirimkan **HTTP Status Code**. Kita bisa cek status ini menggunakan `ajax.status`.

---

## 📊 HTTP Status Code yang Umum

| Status Code | Arti | 
|-------------|------|
| **200** | ✅ Berhasil |
| **404** | 🔍 Data tidak ditemukan |
| **500** | 💥 Error di server |

---

## 💻 Contoh Sederhana

### Contoh 1: ✅ Request Berhasil

```javascript
const ajax = new XMLHttpRequest();
ajax.open("GET", "https://jsonplaceholder.typicode.com/users/1", true);

ajax.addEventListener("load", function () {
    if (ajax.status === 200) {
        const data = JSON.parse(ajax.responseText);
        console.log("Berhasil! Nama:", data.name);
    }
});

ajax.send();
```

### 📝 Penjelasan:

1. Buat objek AJAX
2. Setting request ke API
3. Cek `ajax.status === 200` (berhasil)
4. Ambil dan tampilkan data

### 🖥️ Output:

```
Berhasil! Nama: Leanne Graham
```

---

### Contoh 2: ❌ Menangani Error

```javascript
const ajax = new XMLHttpRequest();
ajax.open("GET", "https://jsonplaceholder.typicode.com/users/1", true);

ajax.addEventListener("load", function () {
    if (ajax.status === 200) {
        const data = JSON.parse(ajax.responseText);
        console.log("✅ Berhasil! Nama:", data.name);
    } else {
        console.log("❌ Error! Status:", ajax.status);
    }
});

ajax.send();
```

### 📝 Penjelasan:

- Tambahkan `else` untuk menangani error
- Tampilkan status code jika gagal

### 🖥️ Output (jika berhasil):

```
✅ Berhasil! Nama: Leanne Graham
```

### 🖥️ Output (jika gagal):

```
❌ Error! Status: 404
```

---

### Contoh 3: 🎨 Tampilkan di Halaman Web

```javascript
const ajax = new XMLHttpRequest();
ajax.open("GET", "https://jsonplaceholder.typicode.com/users/1", true);

ajax.addEventListener("load", function () {
    if (ajax.status === 200) {
        const data = JSON.parse(ajax.responseText);
        document.body.innerHTML = "<h2>✅ " + data.name + "</h2>";
    } else {
        document.body.innerHTML = "<h2>❌ Error!</h2>";
    }
});

ajax.send();
```

### 📝 Penjelasan:

- Tampilkan hasil di halaman web
- Gunakan `document.body.innerHTML`

### 🖥️ Output di Browser:

**Jika berhasil:**
```
✅ Leanne Graham
```

**Jika gagal:**
```
❌ Error!
```

---

## 🎓 Kesimpulan

### 📌 Yang Perlu Diingat:

1. ✅ Gunakan `ajax.status` untuk cek status
2. 🎯 Status **200** = Berhasil
3. ⚠️ Status lainnya = Ada error
4. 🔍 Selalu gunakan **if-else** untuk handle error

---

## 📚 Referensi

👉 [MDN - XMLHttpRequest.status](https://developer.mozilla.org/en-US/docs/Web/API/XMLHttpRequest/status)

---

**Happy Coding! 🎉**
