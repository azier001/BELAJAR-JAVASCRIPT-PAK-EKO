# 📚 JavaScript Async & Callback

## 🎯 Apa itu Callback?

**Callback** adalah mekanisme fundamental dalam JavaScript untuk menangani operasi asynchronous.

### 💡 Penjelasan

- **Callback** merupakan mekanisme untuk memanggil kembali kode yang ada di program dari proses Async

- Callback biasanya dibuat dalam bentuk **function as parameter**, dan parameter function tersebut akan dieksekusi saat proses Async selesai

- Dengan menggunakan Callback, program bisa menerima informasi yang dibutuhkan dari proses yang berjalan secara Async

### 📝 Contoh Sederhana Callback

```javascript
function selesaiBelajar(nama, callback) {
    console.log(`${nama} sedang belajar...`);
    // Simulasi proses async
    callback(nama);
}

function ucapanSelamat(nama) {
    console.log(`Selamat ${nama}! Kamu sudah selesai belajar! 🎉`);
}

// Memanggil function dengan callback
selesaiBelajar("Budi", ucapanSelamat);
```

**Penjelasan Kode:**
- `selesaiBelajar()` adalah function yang menerima parameter `nama` dan `callback`
- `ucapanSelamat()` adalah function yang akan dijadikan callback
- Ketika `selesaiBelajar()` dipanggil, ia akan menjalankan `callback(nama)` yang merujuk ke function `ucapanSelamat()`

**Output:**
```
Budi sedang belajar...
Selamat Budi! Kamu sudah selesai belajar! 🎉
```

---

## ⚡ Async Method

Ada banyak method Async yang terdapat di JavaScript, yang akan kita bahas kali ini adalah:

### ⏰ setTimeout()

**setTimeout(callback, timeInMilis)** digunakan untuk menjalankan proses Async **sekali** dalam waktu tertentu.

#### 💡 Penjelasan

Method ini akan menunda eksekusi function callback selama waktu yang ditentukan (dalam milidetik). Setelah waktu habis, callback akan dijalankan **satu kali saja**.

#### 📝 Contoh setTimeout

```javascript
console.log("Mulai! ⏰");

setTimeout(function() {
    console.log("Pesan ini muncul setelah 2 detik! ⏳");
}, 2000);

console.log("Selesai! ✅");
```

**Penjelasan Kode:**
- `setTimeout()` menerima 2 parameter: function callback dan waktu delay (2000 ms = 2 detik)
- Kode tidak menunggu setTimeout selesai, melainkan langsung melanjutkan ke baris berikutnya
- Ini menunjukkan sifat **asynchronous** dari setTimeout

**Output:**
```
Mulai! ⏰
Selesai! ✅
Pesan ini muncul setelah 2 detik! ⏳
```

> **Perhatikan:** Output "Selesai!" muncul **sebelum** pesan dari setTimeout, karena setTimeout berjalan secara async!

---

### 🔄 setInterval()

**setInterval(callback, timeInMilis)** digunakan untuk menjalankan proses Async secara **periodik** dalam waktu tertentu.

#### 💡 Penjelasan

Method ini akan menjalankan function callback **berulang-ulang** setiap interval waktu yang ditentukan, sampai dihentikan dengan `clearInterval()`.

#### 📝 Contoh setInterval

```javascript
let hitungan = 0;

console.log("Memulai interval... 🚀");

const intervalId = setInterval(function() {
    hitungan++;
    console.log(`Hitungan ke-${hitungan} ⏱️`);
    
    // Hentikan setelah 5 kali
    if (hitungan === 5) {
        clearInterval(intervalId);
        console.log("Interval dihentikan! 🛑");
    }
}, 1000);
```

**Penjelasan Kode:**
- `setInterval()` menjalankan callback setiap 1000 ms (1 detik)
- `intervalId` menyimpan referensi ke interval yang sedang berjalan
- `clearInterval(intervalId)` digunakan untuk menghentikan interval
- Tanpa `clearInterval()`, interval akan terus berjalan selamanya

**Output:**
```
Memulai interval... 🚀
Hitungan ke-1 ⏱️
Hitungan ke-2 ⏱️
Hitungan ke-3 ⏱️
Hitungan ke-4 ⏱️
Hitungan ke-5 ⏱️
Interval dihentikan! 🛑
```

---

## 🎓 Kesimpulan

| Method | Frekuensi | Cara Hentikan | Kegunaan |
|--------|-----------|---------------|----------|
| `setTimeout()` | Satu kali ⚡ | Otomatis selesai | Delay eksekusi kode |
| `setInterval()` | Berulang 🔄 | `clearInterval()` | Eksekusi periodik |

> **💡 Tips:** Gunakan setTimeout untuk tugas yang hanya perlu dijalankan sekali setelah delay, dan setInterval untuk tugas yang perlu dijalankan berulang kali!
