# 🪟 **Window Object di JavaScript**

`Window` merupakan **representasi dari jendela browser** yang berisi **DOM Document** dan berbagai elemen lainnya. Objek ini adalah bagian penting dari lingkungan JavaScript di browser.

---

## 🌐 Apa Itu Window?

* `Window` adalah **global object** yang secara otomatis tersedia di browser.
* Semua variabel global, function, dan object yang dibuat di JavaScript akan menjadi **property dari object `window`**.

💡 **Catatan:** Kamu tidak perlu membuat object `window` secara manual, karena sudah otomatis tersedia.

📘 **Contoh:**

```javascript
console.log(window);
```

🧩 **Penjelasan:**
Kode di atas akan menampilkan seluruh isi object `window` di console browser.

💻 **Output:**

```
Window { ... banyak property dan method ... }
```

---

## ⚙️ Beberapa JavaScript Function dari Window

Beberapa function JavaScript yang sering digunakan sebenarnya berasal dari object `window`.

* `alert()` → menampilkan pesan ke pengguna.
* `confirm()` → meminta konfirmasi dari pengguna (OK/Cancel).
* `prompt()` → meminta input teks dari pengguna.

📘 **Contoh:**

```javascript
alert('Halo, Dunia!'); // menampilkan pesan
const yakin = confirm('Apakah kamu yakin?'); // konfirmasi
const nama = prompt('Masukkan nama kamu:'); // input teks
```

🧩 **Penjelasan:**

* `alert()` akan menampilkan pop-up sederhana.
* `confirm()` mengembalikan `true` jika pengguna menekan OK, dan `false` jika Cancel.
* `prompt()` mengembalikan teks yang dimasukkan pengguna.

💻 **Output (contoh simulasi):**

```
alert → Menampilkan kotak dialog: "Halo, Dunia!"
confirm → true (jika pengguna klik OK)
prompt → "Asroqi" (jika pengguna mengetik nama)
```

---

## 🔧 Property, Method, dan Event pada Window

Object `window` memiliki **banyak sekali property, method, dan event** yang bisa digunakan.

📘 **Contoh umum:**

```javascript
console.log(window.innerWidth);  // lebar viewport
console.log(window.location.href); // URL saat ini
window.open('https://developer.mozilla.org'); // buka tab baru
```

🧩 **Penjelasan:**

* `window.innerWidth` → mengembalikan lebar tampilan browser dalam piksel.
* `window.location.href` → mengembalikan URL halaman yang sedang dibuka.
* `window.open()` → membuka jendela atau tab baru.

💻 **Output:**

```
innerWidth → 1366
location.href → "https://example.com"
open() → membuka tab baru ke MDN
```

---

## 💡 Cara Menggunakan Window

Kita **tidak perlu menuliskan kata `window`** setiap kali ingin mengakses property atau method-nya — JavaScript otomatis mengenalinya.

📘 **Contoh:**

```javascript
alert('Hello');
// sama saja dengan
window.alert('Hello');
```

💻 **Output:**

```
Menampilkan kotak dialog: "Hello"
```

🧠 **Kesimpulan:**

* `window` adalah **global object utama** di browser.
* Semua variabel global, function, dan objek DOM terkait berada di bawah `window`.
* Kita dapat mengakses berbagai fitur browser (seperti ukuran layar, lokasi halaman, popup, dan lain-lain) melalui `window`.

---

📚 **Referensi:**
🔗 [MDN Web Docs: Window API](https://developer.mozilla.org/en-US/docs/Web/API/Window)
