# 🎯 Event Handler

> **Event Handler** adalah mekanisme yang memungkinkan Node untuk bereaksi terhadap berbagai kejadian atau interaksi yang terjadi pada elemen HTML.

## 📋 Pengenalan Event Handler

Event Handler memungkinkan kita untuk membuat aplikasi web yang **interaktif** dan **responsif** terhadap aksi pengguna.

### ✨ Apa itu Event Handler?

- 🖱️ Node memiliki kemampuan bereaksi terhadap suatu kejadian, misal kejadian di klik, mouse di atas node tersebut, dan lain-lain
- 🎨 Ada banyak sekali jenis kejadian yang bisa kita buatkan aksinya ketika kejadian tersebut terjadi, atau dikenal dengan nama **Event Handler**
- 🔧 Ada dua cara kita menambahkan Event Handler ke Node, menggunakan **Event Target** atau **Global Event Handler**

### 📚 Referensi MDN

- 🔗 [Event Target Documentation](https://developer.mozilla.org/en-US/docs/Web/API/EventTarget)
- 🔗 [Global Event Handlers Documentation](https://developer.mozilla.org/en-US/docs/Web/API/GlobalEventHandlers)

---

## 🎯 Event Target

> **Event Target** adalah parent class dari Node yang menyediakan kemampuan untuk menangani event pada elemen HTML.

### 📖 Penjelasan Event Target

- 🌳 Event Target merupakan **parent class** dari Node, artinya semua Node memiliki kemampuan dari Event Target
- ⚙️ Pada Event Target, kita bisa menggunakan method `addEventListener(type, callback)` untuk menambahkan Event Handler
- 🔗 [Event Target API Reference](https://developer.mozilla.org/en-US/docs/Web/API/EventTarget)

### 💡 Keuntungan Menggunakan Event Target

- ✅ Dapat menambahkan **multiple event handlers** untuk satu event yang sama
- ✅ Lebih **fleksibel** dan mudah untuk remove event handler dengan `removeEventListener()`
- ✅ Mendukung **event capturing** dan **bubbling phase**
- ✅ Best practice dalam modern JavaScript

### 🔨 Contoh Penggunaan Event Target

#### Contoh 1: Click Event dengan addEventListener

```html
<!DOCTYPE html>
<html>
<head>
    <title>Event Target - Click Example</title>
</head>
<body>
    <button id="myButton">Klik Saya!</button>
    <p id="output"></p>

    <script>
        // Mengambil elemen button
        const button = document.getElementById('myButton');
        const output = document.getElementById('output');
        
        // Menambahkan event listener untuk event 'click'
        button.addEventListener('click', function() {
            output.textContent = '🎉 Button telah diklik!';
            console.log('Button clicked!');
        });
    </script>
</body>
</html>
```

**📝 Penjelasan Kode:**
- `getElementById()` digunakan untuk mengambil elemen button dan paragraph
- `addEventListener('click', callback)` menambahkan event handler untuk event click
- Ketika button diklik, fungsi callback akan dijalankan
- Fungsi callback mengubah text content dari paragraph

**✅ Output:**
- Sebelum diklik: Halaman menampilkan button dengan text "Klik Saya!" dan paragraph kosong
- Setelah diklik: Paragraph menampilkan "🎉 Button telah diklik!" dan console menampilkan "Button clicked!"

---

#### Contoh 2: Multiple Event Listeners

```html
<!DOCTYPE html>
<html>
<head>
    <title>Multiple Event Listeners</title>
    <style>
        #box {
            width: 200px;
            height: 200px;
            background-color: lightblue;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 24px;
            cursor: pointer;
            transition: all 0.3s;
        }
    </style>
</head>
<body>
    <div id="box">Hover & Click Me!</div>
    <p id="status"></p>

    <script>
        const box = document.getElementById('box');
        const status = document.getElementById('status');
        
        // Event listener pertama - untuk click
        box.addEventListener('click', function() {
            status.textContent = '🖱️ Box diklik!';
            box.style.backgroundColor = 'lightcoral';
        });
        
        // Event listener kedua - untuk mouseover
        box.addEventListener('mouseover', function() {
            status.textContent = '👆 Mouse berada di atas box';
            box.style.transform = 'scale(1.1)';
        });
        
        // Event listener ketiga - untuk mouseout
        box.addEventListener('mouseout', function() {
            status.textContent = '👋 Mouse keluar dari box';
            box.style.transform = 'scale(1)';
        });
    </script>
</body>
</html>
```

**📝 Penjelasan Kode:**
- Tiga event listener berbeda ditambahkan ke elemen yang sama
- `click` event mengubah warna background menjadi lightcoral
- `mouseover` event memperbesar box dengan transform scale
- `mouseout` event mengembalikan ukuran box ke normal
- Setiap event memperbarui status text

**✅ Output:**
- Saat mouse hover: Box membesar 1.1x, status "👆 Mouse berada di atas box"
- Saat diklik: Background berubah merah muda, status "🖱️ Box diklik!"
- Saat mouse keluar: Box kembali normal, status "👋 Mouse keluar dari box"

---

#### Contoh 3: Event Object dan preventDefault

```html
<!DOCTYPE html>
<html>
<head>
    <title>Event Object Example</title>
</head>
<body>
    <form id="myForm">
        <label>Nama: <input type="text" id="name" required></label><br><br>
        <button type="submit">Submit</button>
    </form>
    <p id="result"></p>

    <script>
        const form = document.getElementById('myForm');
        const result = document.getElementById('result');
        
        form.addEventListener('submit', function(event) {
            // Mencegah form untuk submit (reload page)
            event.preventDefault();
            
            // Mengambil nilai dari input
            const name = document.getElementById('name').value;
            
            // Menampilkan informasi event
            result.innerHTML = `
                ✅ Form submitted!<br>
                👤 Nama: ${name}<br>
                🎯 Event Type: ${event.type}<br>
                📍 Target: ${event.target.tagName}
            `;
            
            console.log('Event Object:', event);
        });
    </script>
</body>
</html>
```

**📝 Penjelasan Kode:**
- `event.preventDefault()` mencegah behavior default form (reload page)
- Parameter `event` adalah objek yang berisi informasi tentang event
- `event.type` menampilkan jenis event (dalam hal ini: "submit")
- `event.target` merujuk ke elemen yang memicu event

**✅ Output:**
- Ketika form disubmit: Halaman tidak reload
- Tampilan menunjukkan:
  - "✅ Form submitted!"
  - Nama yang diinput user
  - Event type: "submit"
  - Target element: "FORM"

---

## 🌐 Global Event Handler

> **Global Event Handler** adalah cara alternatif untuk menambahkan event handler dengan menggunakan property pada elemen HTML.

### 📖 Penjelasan Global Event Handler

- 🔄 Selain menggunakan Event Target, untuk menambahkan Event Handler, kita juga bisa menggunakan **Global Event Handler**
- 📋 Global Event Handler bukanlah sebuah super class, hanya **kontrak** yang bisa kita gunakan untuk menambah event handler sesuai dengan type event nya
- 🔗 [Global Event Handlers API Reference](https://developer.mozilla.org/en-US/docs/Web/API/GlobalEventHandlers)

### 🔍 Karakteristik Global Event Handler

- 🎯 Menggunakan property seperti `onclick`, `onmouseover`, `onkeydown`, dll
- ⚠️ Hanya dapat menambahkan **satu handler per event type**
- 🔄 Handler baru akan **menimpa** handler sebelumnya
- 📝 Sintaks lebih sederhana untuk kasus-kasus simple

### 🔨 Contoh Penggunaan Global Event Handler

#### Contoh 1: Click Event dengan onclick

```html
<!DOCTYPE html>
<html>
<head>
    <title>Global Event Handler - onclick</title>
</head>
<body>
    <button id="clickButton">Klik Saya!</button>
    <p id="clickOutput"></p>

    <script>
        const button = document.getElementById('clickButton');
        const output = document.getElementById('clickOutput');
        
        // Menggunakan onclick property
        button.onclick = function() {
            output.textContent = '🎊 Button telah diklik menggunakan onclick!';
            output.style.color = 'green';
        };
    </script>
</body>
</html>
```

**📝 Penjelasan Kode:**
- `onclick` adalah property dari Global Event Handler
- Fungsi langsung diassign ke property `onclick`
- Ketika button diklik, fungsi akan dieksekusi
- Lebih simple dibanding `addEventListener` untuk single handler

**✅ Output:**
- Setelah button diklik: Text "🎊 Button telah diklik menggunakan onclick!" muncul dengan warna hijau

---

#### Contoh 2: Mouse Events dengan Global Handler

```html
<!DOCTYPE html>
<html>
<head>
    <title>Global Event Handler - Mouse Events</title>
    <style>
        #hoverBox {
            width: 250px;
            height: 150px;
            background-color: #3498db;
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 18px;
            border-radius: 10px;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <div id="hoverBox">Hover dan Klik Saya!</div>
    <p id="eventInfo"></p>

    <script>
        const box = document.getElementById('hoverBox');
        const info = document.getElementById('eventInfo');
        
        // onmouseover - ketika mouse masuk ke area element
        box.onmouseover = function() {
            box.style.backgroundColor = '#e74c3c';
            info.textContent = '🖱️ Mouse masuk ke dalam box!';
        };
        
        // onmouseout - ketika mouse keluar dari area element
        box.onmouseout = function() {
            box.style.backgroundColor = '#3498db';
            info.textContent = '👋 Mouse keluar dari box!';
        };
        
        // onclick - ketika element diklik
        box.onclick = function() {
            info.textContent = '🎯 Box telah diklik!';
            box.textContent = 'Clicked!';
            setTimeout(() => {
                box.textContent = 'Hover dan Klik Saya!';
            }, 1000);
        };
    </script>
</body>
</html>
```

**📝 Penjelasan Kode:**
- `onmouseover` dipicu saat mouse masuk ke area elemen
- `onmouseout` dipicu saat mouse keluar dari area elemen
- `onclick` dipicu saat elemen diklik
- `setTimeout` digunakan untuk mengembalikan text setelah 1 detik

**✅ Output:**
- Hover: Box berubah warna merah, info "🖱️ Mouse masuk ke dalam box!"
- Mouse keluar: Box kembali biru, info "👋 Mouse keluar dari box!"
- Klik: Text berubah "Clicked!", info "🎯 Box telah diklik!", lalu kembali normal setelah 1 detik

---

#### Contoh 3: Form Events dengan Global Handler

```html
<!DOCTYPE html>
<html>
<head>
    <title>Global Event Handler - Form Events</title>
    <style>
        .input-group {
            margin: 10px 0;
        }
        input {
            padding: 8px;
            font-size: 14px;
        }
        .error {
            color: red;
            font-size: 12px;
        }
        .success {
            color: green;
            font-size: 12px;
        }
    </style>
</head>
<body>
    <h3>📝 Form dengan Event Handler</h3>
    <div class="input-group">
        <input type="text" id="username" placeholder="Username (min 5 karakter)">
        <p id="usernameMsg"></p>
    </div>
    
    <div class="input-group">
        <input type="email" id="email" placeholder="Email">
        <p id="emailMsg"></p>
    </div>

    <script>
        const username = document.getElementById('username');
        const usernameMsg = document.getElementById('usernameMsg');
        const email = document.getElementById('email');
        const emailMsg = document.getElementById('emailMsg');
        
        // onfocus - ketika input mendapat focus
        username.onfocus = function() {
            usernameMsg.textContent = '✏️ Silakan ketik username Anda...';
            usernameMsg.className = '';
        };
        
        // onblur - ketika input kehilangan focus
        username.onblur = function() {
            if (username.value.length < 5) {
                usernameMsg.textContent = '❌ Username minimal 5 karakter!';
                usernameMsg.className = 'error';
            } else {
                usernameMsg.textContent = '✅ Username valid!';
                usernameMsg.className = 'success';
            }
        };
        
        // oninput - setiap ada perubahan nilai input
        email.oninput = function() {
            const emailPattern = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
            if (emailPattern.test(email.value)) {
                emailMsg.textContent = '✅ Format email valid!';
                emailMsg.className = 'success';
            } else if (email.value.length > 0) {
                emailMsg.textContent = '⚠️ Format email belum valid';
                emailMsg.className = 'error';
            } else {
                emailMsg.textContent = '';
            }
        };
    </script>
</body>
</html>
```

**📝 Penjelasan Kode:**
- `onfocus` dipicu saat input field mendapat focus (diklik/tab)
- `onblur` dipicu saat input field kehilangan focus
- `oninput` dipicu setiap kali ada perubahan nilai pada input (real-time)
- Validasi username mengecek panjang minimal 5 karakter
- Validasi email menggunakan regex pattern

**✅ Output:**
- Klik username field: Muncul pesan "✏️ Silakan ketik username Anda..."
- Keluar dari username dengan <5 karakter: "❌ Username minimal 5 karakter!" (merah)
- Keluar dari username dengan ≥5 karakter: "✅ Username valid!" (hijau)
- Ketik email: Real-time validation menunjukkan format valid/invalid

---

## 🆚 Perbandingan Event Target vs Global Event Handler

| Aspek | Event Target (`addEventListener`) | Global Event Handler (`onclick`, dll) |
|-------|-----------------------------------|---------------------------------------|
| 🔢 **Multiple Handlers** | ✅ Bisa menambahkan banyak handler | ❌ Hanya satu handler per event |
| 🗑️ **Remove Handler** | ✅ Ada `removeEventListener()` | ⚠️ Set ke `null` untuk remove |
| 🎯 **Event Capturing** | ✅ Mendukung capturing phase | ❌ Tidak mendukung |
| 💻 **Modern Practice** | ✅ Recommended | ⚠️ Legacy approach |
| 📝 **Sintaks** | Lebih verbose | Lebih simple |
| 🔧 **Fleksibilitas** | Sangat fleksibel | Terbatas |

### 💡 Kapan Menggunakan Masing-Masing?

**Gunakan Event Target (`addEventListener`)** ✅
- Ketika butuh multiple handlers untuk satu event
- Untuk aplikasi kompleks dan scalable
- Ketika butuh kontrol lebih (capturing, once option, dll)
- Best practice untuk modern JavaScript

**Gunakan Global Event Handler** ⚙️
- Untuk prototype atau testing cepat
- Script sederhana dengan single handler
- Ketika butuh sintaks yang lebih ringkas

---

## 🎓 Kesimpulan

- 🎯 **Event Handler** memungkinkan interaksi dinamis antara user dan aplikasi web
- 🌳 **Event Target** lebih fleksibel dan merupakan cara modern untuk handle events
- 🌐 **Global Event Handler** lebih simple tapi terbatas untuk single handler
- 💪 Pahami kedua metode untuk memilih approach yang tepat sesuai kebutuhan
- 🚀 Practice dengan berbagai jenis event untuk menguasai event handling!

### 📌 Tips Best Practice

1. ✅ Prefer `addEventListener` untuk production code
2. 🎯 Gunakan event delegation untuk performa lebih baik
3. 🧹 Selalu cleanup event listeners yang tidak dibutuhkan
4. 📝 Beri nama function handler yang deskriptif
5. ⚡ Gunakan `event.preventDefault()` dengan bijak

---

**🔗 Referensi Lengkap:**
- [MDN - Event Target](https://developer.mozilla.org/en-US/docs/Web/API/EventTarget)
- [MDN - Global Event Handlers](https://developer.mozilla.org/en-US/docs/Web/API/GlobalEventHandlers)
- [MDN - Event Reference](https://developer.mozilla.org/en-US/docs/Web/Events)

**Happy Coding! 🚀✨**
