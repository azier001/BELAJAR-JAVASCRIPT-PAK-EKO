# 🧩 Text Content, Inner Text, dan Inner HTML

Dokumentasi ini menjelaskan tiga properti penting dalam JavaScript DOM yang digunakan untuk **mengambil** atau **mengubah** isi teks dan HTML dari sebuah elemen.

---

## 📝 1. Text Content dan Inner Text

### 📖 Deskripsi

Property `textContent` dan `innerText` digunakan untuk **mengambil atau mengubah isi teks** dari sebuah elemen. Meskipun keduanya terlihat mirip, ada perbedaan penting dalam cara mereka bekerja.

### 🔍 Perbedaan Utama

* **`textContent`**: Mengembalikan semua teks dari elemen, termasuk teks yang tersembunyi oleh CSS (`display: none`).
* **`innerText`**: Hanya mengembalikan teks yang **terlihat** di halaman (mengabaikan teks yang disembunyikan oleh CSS).

### 💡 Contoh Kode

```html
<p id="contoh">Halo <span style="display:none">Dunia</span>!</p>
<script>
const elemen = document.getElementById("contoh");

console.log(elemen.textContent); // Output: "Halo Dunia!"
console.log(elemen.innerText);   // Output: "Halo"
</script>
```

### 🧠 Penjelasan

* `textContent` membaca semua teks di dalam elemen, bahkan teks yang tidak terlihat.
* `innerText` hanya menampilkan teks yang tampil di layar.

### 🖨️ Output

```
Halo Dunia!
Halo
```

---

## 🌐 2. Inner HTML

### 📖 Deskripsi

Berbeda dari `textContent` dan `innerText`, properti `innerHTML` digunakan untuk **mengambil atau mengubah isi elemen beserta tag HTML-nya**.

### 🧩 Kegunaan

* Cocok digunakan jika kita ingin **memasukkan atau membaca struktur HTML** di dalam sebuah elemen.

### 💡 Contoh Kode

```html
<div id="box"><b>Selamat Datang</b></div>
<script>
const elemen = document.getElementById("box");

console.log(elemen.innerHTML); // Output: "<b>Selamat Datang</b>"
</script>
```

### 🧠 Penjelasan

* `innerHTML` mengembalikan teks **beserta tag HTML-nya**.
* Nilai ini bisa diubah untuk mengganti isi elemen secara langsung dengan HTML baru.

### 🖨️ Output

```
<b>Selamat Datang</b>
```

---

## 🔧 3. Mengubah Element dengan Inner HTML

### 📖 Deskripsi

Salah satu kekuatan utama dari `innerHTML` adalah kemampuannya untuk **mengubah isi anak elemen (children)** hanya dengan sebuah string yang berisi HTML.

### 💡 Contoh Kode

```html
<div id="kontainer"></div>
<script>
const kontainer = document.getElementById("kontainer");
kontainer.innerHTML = "<h3>Judul Baru</h3><p>Ini adalah paragraf baru.</p>";
</script>
```

### 🧠 Penjelasan

* String yang dimasukkan ke dalam `innerHTML` akan **di-render sebagai HTML**.
* Tag yang terdapat di dalam string otomatis menjadi **child element** dari elemen target.

### 🖨️ Output di Browser

```
Judul Baru
Ini adalah paragraf baru.
```

Teks ini akan tampil seperti elemen HTML biasa, bukan sebagai teks mentah.

---

## 🎯 Kesimpulan

| Properti      | Mengambil Teks        | Menyertakan HTML | Memperhatikan Tampilan |
| ------------- | --------------------- | ---------------- | ---------------------- |
| `textContent` | ✅ Semua teks          | ❌ Tidak          | ❌ Tidak                |
| `innerText`   | ✅ Hanya teks terlihat | ❌ Tidak          | ✅ Ya                   |
| `innerHTML`   | ❌ Tidak               | ✅ Ya             | ❌ Tidak                |

✨ Gunakan properti yang sesuai dengan kebutuhan — apakah kamu hanya ingin mengambil teks, atau juga struktur HTML di dalam elemen!
