# 📊 HTML Table Element

> 💡 **Catatan Penting**: Salah satu HTML Element yang kompleks selain Form adalah Table

## 🎯 Pengenalan

HTML Table Element adalah salah satu komponen paling kompleks dalam HTML, selain Form. Element ini digunakan untuk menampilkan data dalam format baris dan kolom yang terstruktur.

### ✨ Karakteristik Utama

- **Kompleksitas Tinggi**: Table memiliki struktur yang lebih rumit dibandingkan element HTML lainnya
- **Kaya Fitur**: Memiliki banyak sekali property, method, dan event yang khusus
- **Fleksibel**: Dapat menampung berbagai jenis data dengan format yang terorganisir

---

## 📚 Dokumentasi Resmi

Untuk informasi lengkap tentang HTMLTableElement, kunjungi:
- 🔗 [MDN Web Docs - HTMLTableElement](https://developer.mozilla.org/en-US/docs/Web/API/HTMLTableElement)

---

## 🏗️ Struktur Dasar Table

Table HTML terdiri dari beberapa element penting:

- `<table>` - Container utama untuk seluruh tabel
- `<thead>` - Header/kepala tabel
- `<tbody>` - Body/isi tabel
- `<tfoot>` - Footer/kaki tabel
- `<tr>` - Table Row (baris)
- `<th>` - Table Header (sel header)
- `<td>` - Table Data (sel data)

---

## 💻 Contoh 1: Table Sederhana

### Kode:

```html
<!DOCTYPE html>
<html>
<head>
    <title>Contoh Table Sederhana</title>
    <style>
        table {
            border-collapse: collapse;
            width: 100%;
        }
        th, td {
            border: 1px solid black;
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #4CAF50;
            color: white;
        }
    </style>
</head>
<body>
    <h2>Daftar Mahasiswa</h2>
    <table>
        <thead>
            <tr>
                <th>No</th>
                <th>Nama</th>
                <th>NIM</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>1</td>
                <td>Budi Santoso</td>
                <td>12345</td>
            </tr>
            <tr>
                <td>2</td>
                <td>Ani Wijaya</td>
                <td>12346</td>
            </tr>
        </tbody>
    </table>
</body>
</html>
```

### 📝 Penjelasan Kode:

- `<table>` membuat container untuk tabel
- `<thead>` mendefinisikan bagian header tabel
- `<th>` membuat sel header dengan teks tebal
- `<tbody>` mendefinisikan bagian isi tabel
- `<td>` membuat sel data biasa
- CSS `border-collapse: collapse` menggabungkan border agar tidak double

### 🖼️ Output:

```
┌────┬──────────────┬───────┐
│ No │ Nama         │ NIM   │
├────┼──────────────┼───────┤
│ 1  │ Budi Santoso │ 12345 │
│ 2  │ Ani Wijaya   │ 12346 │
└────┴──────────────┴───────┘
```

---

## 💻 Contoh 2: Menggunakan JavaScript dengan HTMLTableElement

### Kode:

```html
<!DOCTYPE html>
<html>
<head>
    <title>Table dengan JavaScript</title>
    <style>
        table {
            border-collapse: collapse;
            width: 100%;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #2196F3;
            color: white;
        }
        tr:hover {
            background-color: #f5f5f5;
        }
    </style>
</head>
<body>
    <h2>Daftar Produk</h2>
    <button onclick="tambahBaris()">➕ Tambah Produk</button>
    <table id="tabelProduk">
        <thead>
            <tr>
                <th>Nama Produk</th>
                <th>Harga</th>
                <th>Stok</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>Laptop</td>
                <td>Rp 5.000.000</td>
                <td>10</td>
            </tr>
        </tbody>
    </table>

    <script>
        function tambahBaris() {
            // Mengakses table menggunakan HTMLTableElement
            const table = document.getElementById('tabelProduk');
            
            // Mengakses tbody
            const tbody = table.getElementsByTagName('tbody')[0];
            
            // Membuat baris baru
            const barisBaru = tbody.insertRow();
            
            // Menambahkan sel
            const sel1 = barisBaru.insertCell(0);
            const sel2 = barisBaru.insertCell(1);
            const sel3 = barisBaru.insertCell(2);
            
            // Mengisi data
            sel1.textContent = 'Mouse';
            sel2.textContent = 'Rp 150.000';
            sel3.textContent = '25';
            
            alert('✅ Produk baru berhasil ditambahkan!');
        }
    </script>
</body>
</html>
```

### 📝 Penjelasan Kode:

- `getElementById()` mengakses table berdasarkan ID
- `getElementsByTagName('tbody')[0]` mengambil element tbody pertama
- `insertRow()` menambahkan baris baru ke tbody
- `insertCell(index)` menambahkan sel ke baris pada posisi tertentu
- `textContent` mengisi konten sel dengan teks

### 🖼️ Output Awal:

```
┌─────────────┬──────────────┬──────┐
│ Nama Produk │ Harga        │ Stok │
├─────────────┼──────────────┼──────┤
│ Laptop      │ Rp 5.000.000 │ 10   │
└─────────────┴──────────────┴──────┘
```

### 🖼️ Output Setelah Klik Tombol:

```
┌─────────────┬──────────────┬──────┐
│ Nama Produk │ Harga        │ Stok │
├─────────────┼──────────────┼──────┤
│ Laptop      │ Rp 5.000.000 │ 10   │
│ Mouse       │ Rp 150.000   │ 25   │
└─────────────┴──────────────┴──────┘
```

---

## 🎨 Property Penting HTMLTableElement

### 1️⃣ **rows**
Mengembalikan koleksi semua baris dalam tabel

```javascript
const table = document.getElementById('myTable');
console.log(table.rows.length); // Jumlah baris
```

### 2️⃣ **tBodies**
Mengembalikan koleksi semua tbody element

```javascript
console.log(table.tBodies.length); // Jumlah tbody
```

### 3️⃣ **tHead**
Mengembalikan element thead

```javascript
console.log(table.tHead); // Element <thead>
```

---

## 🔧 Method Penting HTMLTableElement

### 1️⃣ **insertRow(index)**
Menambahkan baris baru pada posisi tertentu

```javascript
const barisBaru = table.insertRow(0); // Tambah di awal
```

### 2️⃣ **deleteRow(index)**
Menghapus baris pada posisi tertentu

```javascript
table.deleteRow(0); // Hapus baris pertama
```

### 3️⃣ **createTHead()**
Membuat element thead jika belum ada

```javascript
const thead = table.createTHead();
```

---

## 🎯 Tips & Best Practices

- ✅ Gunakan `<thead>`, `<tbody>`, dan `<tfoot>` untuk struktur yang jelas
- ✅ Tambahkan CSS untuk styling yang menarik
- ✅ Gunakan `border-collapse: collapse` untuk border yang rapi
- ✅ Manfaatkan JavaScript untuk manipulasi data dinamis
- ✅ Selalu pertimbangkan responsivitas untuk mobile

---

## 📌 Kesimpulan

HTMLTableElement adalah komponen powerful untuk menampilkan data tabular. Dengan memahami structure, property, method, dan event-nya, kita dapat membuat tabel yang interaktif dan dinamis! 🚀
