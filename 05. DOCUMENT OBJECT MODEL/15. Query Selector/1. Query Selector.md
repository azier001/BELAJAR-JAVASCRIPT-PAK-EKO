# 🌟 Panduan Lengkap Query Selector di JavaScript

---

## 🧠 Apa Itu Query Selector?

Query Selector adalah **method JavaScript** yang digunakan untuk menyeleksi node atau elemen HTML berdasarkan **pola selector CSS**.

Terdapat dua method utama:

* 🟢 **`document.querySelector(pola)`** → Menyeleksi **elemen pertama** yang sesuai dengan pola.
* 🟣 **`document.querySelectorAll(pola)`** → Menyeleksi **semua elemen** yang sesuai dengan pola.

📘 Contoh:

```html
<p class="text">Halo Dunia!</p>
<p class="text">Selamat Datang!</p>
<script>
  const pertama = document.querySelector('.text');
  const semua = document.querySelectorAll('.text');
  console.log(pertama.textContent); // Output: Halo Dunia!
  console.log(semua.length); // Output: 2
</script>
```

💬 **Penjelasan:**

* `querySelector()` hanya mengambil elemen pertama yang cocok.
* `querySelectorAll()` mengembalikan semua elemen yang cocok dalam bentuk NodeList.

---

## 🎨 Pola Query Selector

Pola atau pattern dalam Query Selector mengikuti **CSS Selector Syntax**. Artinya, cara kita menyeleksi elemen di CSS sama dengan cara kita menyeleksinya di JavaScript menggunakan Query Selector.

🔗 Referensi: [MDN CSS Selectors](https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_Selectors)

---

## 🌍 Universal Selector (`*`)

Universal Selector digunakan untuk menyeleksi **semua elemen** di dalam dokumen HTML.

📘 Contoh:

```html
<div>Div</div>
<p>Paragraf</p>
<span>Span</span>
<script>
  const semuaElemen = document.querySelectorAll('*');
  console.log(semuaElemen.length); // Output: Jumlah total elemen di halaman
</script>
```

💬 **Penjelasan:**

* Simbol `*` menyeleksi seluruh elemen tanpa terkecuali.

🔗 Referensi: [Universal Selectors – MDN](https://developer.mozilla.org/en-US/docs/Web/CSS/Universal_selectors)

---

## 🏷️ Type Selector (Tag Selector)

Type Selector digunakan untuk menyeleksi elemen berdasarkan **nama tag HTML-nya**.

📘 Contoh:

```html
<p>Halo</p>
<p>Selamat Datang</p>
<script>
  const paragraf = document.querySelectorAll('p');
  console.log(paragraf.length); // Output: 2
</script>
```

💬 **Penjelasan:**

* Selector `p` menyeleksi semua elemen paragraf (`<p>`).

🔗 Referensi: [Type Selectors – MDN](https://developer.mozilla.org/en-US/docs/Web/CSS/Type_selectors)

---

## 🎯 Class Selector (`.`)

Class Selector digunakan untuk menyeleksi elemen berdasarkan **nama class**.

📘 Contoh:

```html
<div class="box">Kotak 1</div>
<div class="box">Kotak 2</div>
<script>
  const kotak = document.querySelectorAll('.box');
  console.log(kotak[0].textContent); // Output: Kotak 1
</script>
```

💬 **Penjelasan:**

* Gunakan tanda titik (`.`) diikuti dengan nama class untuk menyeleksi elemen yang memiliki class tersebut.

🔗 Referensi: [Class Selectors – MDN](https://developer.mozilla.org/en-US/docs/Web/CSS/Class_selectors)

---

## 🆔 ID Selector (`#`)

ID Selector digunakan untuk menyeleksi elemen berdasarkan **atribut `id`**.

📘 Contoh:

```html
<h1 id="judul">Belajar JavaScript</h1>
<script>
  const judul = document.querySelector('#judul');
  console.log(judul.textContent); // Output: Belajar JavaScript
</script>
```

💬 **Penjelasan:**

* Gunakan tanda pagar (`#`) diikuti nama ID.
* Setiap ID seharusnya unik dalam satu dokumen HTML.

🔗 Referensi: [ID Selectors – MDN](https://developer.mozilla.org/en-US/docs/Web/CSS/ID_selectors)

---

## 🧩 Attribute Selector (`[attribute]`)

Attribute Selector digunakan untuk menyeleksi elemen berdasarkan **atribut HTML-nya**.

📘 Contoh:

```html
<input type="text" placeholder="Masukkan nama">
<script>
  const input = document.querySelector('[placeholder]');
  console.log(input.getAttribute('type')); // Output: text
</script>
```

💬 **Penjelasan:**

* Selector `[attribute]` mencari elemen yang memiliki atribut tersebut, tanpa peduli nilainya.
* Dapat juga digunakan dengan nama tag seperti `input[placeholder]`.

🔗 Referensi: [Attribute Selectors – MDN](https://developer.mozilla.org/en-US/docs/Web/CSS/Attribute_selectors)

---

## ⚙️ Operator di Attribute Selector

Kita bisa menggunakan **operator khusus** untuk menyeleksi elemen berdasarkan **nilai atribut tertentu**.

📘 Contoh:

```html
<a href="https://example.com">Website</a>
<a href="https://test.com">Test</a>
<script>
  const linkExample = document.querySelector('[href^="https://example"]');
  console.log(linkExample.textContent); // Output: Website
</script>
```

💬 **Penjelasan Operator:**

| Operator | Contoh            | Deskripsi                                                |
| -------- | ----------------- | -------------------------------------------------------- |
| `=`      | `[type="text"]`   | Mencocokkan atribut dengan nilai yang tepat              |
| `^=`     | `[href^="https"]` | Mencocokkan nilai yang **diawali** dengan teks tertentu  |
| `$=`     | `[href$=".com"]`  | Mencocokkan nilai yang **diakhiri** dengan teks tertentu |
| `*=`     | `[class*="btn"]`  | Mencocokkan nilai yang **mengandung** teks tertentu      |

🔗 Referensi: [Attribute Selectors Syntax – MDN](https://developer.mozilla.org/en-US/docs/Web/CSS/Attribute_selectors#syntax)

---

## 🎉 Kesimpulan

| Selector           | Simbol                            | Deskripsi Singkat              |
| ------------------ | --------------------------------- | ------------------------------ |
| Universal          | `*`                               | Semua elemen                   |
| Type               | `div`, `p`, `span`                | Berdasarkan tag                |
| Class              | `.namaClass`                      | Berdasarkan class              |
| ID                 | `#namaId`                         | Berdasarkan ID                 |
| Attribute          | `[atribut]`                       | Berdasarkan atribut            |
| Attribute Operator | `[attr^=val]`, `[attr*=val]`, dll | Berdasarkan pola nilai atribut |

✨ Dengan Query Selector, kita bisa dengan mudah **mengakses, memanipulasi, dan mengelola elemen HTML** menggunakan pola yang fleksibel dan kuat!
