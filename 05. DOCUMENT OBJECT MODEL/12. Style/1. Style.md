# 🎨 Style - Manipulasi Style dengan DOM

## 📌 Pengenalan

DOM juga bisa digunakan untuk **memanipulasi attribute style** pada element dengan lebih mudah dan efisien.

### 🔑 Keuntungan Menggunakan DOM Style

- ✅ **Lebih Praktis**: Kita tidak perlu menggunakan `setAttribute("style", "value")` setiap ingin mengubah style sebuah element
- ⚡ **Akses Langsung**: Kita bisa langsung gunakan `element.style.name = value`
- 🎯 **Lebih Readable**: Kode menjadi lebih mudah dibaca dan dipahami

### 📚 Referensi

> 🔗 [MDN Web Docs - HTMLElement.style](https://developer.mozilla.org/en-US/docs/Web/API/HTMLElement/style)

---

## 🏷️ Nama Style (Style Naming Convention)

### ⚠️ Perbedaan Penamaan

Saat kita mengubah sebuah style menggunakan DOM, **penamaan style menjadi berubah** dari format CSS biasa.

| Format | Contoh | Digunakan Di |
|--------|--------|--------------|
| **kebab-case** | `background-color` | CSS |
| **camelCase** | `backgroundColor` | JavaScript DOM |

### 📖 Penjelasan

- **Pada style CSS**: Rata-rata menggunakan format **lowercase dan tanda hubung (-)**, misal `background-color`, `font-size`, `border-radius`
- **Pada DOM Style**: Menggunakan format **camelCase**, misal `backgroundColor`, `fontSize`, `borderRadius`

---

## 💡 Contoh Penggunaan

### 1️⃣ Mengubah Warna Background

#### 📝 Kode:

```javascript
// Ambil element dengan id "myDiv"
const myDiv = document.getElementById("myDiv");

// Ubah background color menjadi biru
myDiv.style.backgroundColor = "blue";
```

#### 📋 Penjelasan:
- Kita mengambil element dengan id `myDiv`
- Kemudian mengubah warna background menjadi biru menggunakan `backgroundColor` (bukan `background-color`)

#### 🖥️ Output:
```
Element dengan id "myDiv" akan memiliki background berwarna biru
```

---

### 2️⃣ Mengubah Ukuran Font

#### 📝 Kode:

```javascript
// Ambil element heading
const heading = document.querySelector("h1");

// Ubah ukuran font menjadi 32px
heading.style.fontSize = "32px";
```

#### 📋 Penjelasan:
- Mengambil element `h1` pertama di halaman
- Mengubah ukuran font menjadi 32 pixel menggunakan properti `fontSize`

#### 🖥️ Output:
```
Heading h1 akan tampil dengan ukuran font 32px
```

---

### 3️⃣ Mengubah Multiple Style Sekaligus

#### 📝 Kode:

```javascript
// Ambil element button
const button = document.getElementById("myButton");

// Ubah beberapa style sekaligus
button.style.backgroundColor = "green";
button.style.color = "white";
button.style.padding = "10px 20px";
button.style.border = "none";
button.style.borderRadius = "5px";
```

#### 📋 Penjelasan:
- Mengambil element button dengan id `myButton`
- Mengubah beberapa properti style sekaligus: warna background, warna teks, padding, border, dan border radius
- Setiap properti diubah satu per satu dengan assignment langsung

#### 🖥️ Output:
```
Button akan tampil dengan:
- Background hijau
- Teks putih
- Padding 10px atas-bawah, 20px kiri-kanan
- Tanpa border
- Sudut membulat 5px
```

---

### 4️⃣ Menyembunyikan dan Menampilkan Element

#### 📝 Kode:

```javascript
// Ambil element
const box = document.getElementById("box");

// Sembunyikan element
box.style.display = "none";

// Tampilkan kembali element
box.style.display = "block";
```

#### 📋 Penjelasan:
- Menggunakan properti `display` untuk mengontrol visibility element
- `display = "none"` akan menyembunyikan element sepenuhnya
- `display = "block"` akan menampilkan kembali element

#### 🖥️ Output:
```
Element dengan id "box" akan:
1. Hilang dari tampilan (display: none)
2. Muncul kembali sebagai block element (display: block)
```

---

### 5️⃣ Mengubah Style dengan Conditional

#### 📝 Kode:

```javascript
// Ambil element
const status = document.getElementById("status");
const isActive = true;

// Ubah style berdasarkan kondisi
if (isActive) {
    status.style.color = "green";
    status.style.fontWeight = "bold";
} else {
    status.style.color = "red";
    status.style.fontWeight = "normal";
}
```

#### 📋 Penjelasan:
- Menggunakan conditional statement untuk mengubah style
- Jika `isActive` bernilai true, warna hijau dan bold
- Jika false, warna merah dan normal

#### 🖥️ Output:
```
Karena isActive = true, element "status" akan tampil:
- Warna teks hijau
- Font weight bold
```

---

## 🎯 Tips & Best Practices

### ✨ Yang Perlu Diingat:

1. **📌 Gunakan camelCase** - Selalu ingat konversi dari kebab-case (CSS) ke camelCase (JavaScript)
2. **💬 Gunakan String untuk Nilai** - Nilai style harus dalam bentuk string, contoh: `"20px"`, `"red"`, `"bold"`
3. **🎨 Pisahkan Style Kompleks** - Untuk styling kompleks, lebih baik gunakan class CSS dan toggle class dengan JavaScript
4. **⚡ Performance** - Mengubah banyak style sekaligus bisa mempengaruhi performance, pertimbangkan menggunakan class

### 🚀 Konversi Nama Style yang Sering Digunakan:

| CSS | JavaScript DOM |
|-----|----------------|
| `background-color` | `backgroundColor` |
| `font-size` | `fontSize` |
| `font-weight` | `fontWeight` |
| `margin-top` | `marginTop` |
| `padding-left` | `paddingLeft` |
| `border-radius` | `borderRadius` |
| `text-align` | `textAlign` |
| `line-height` | `lineHeight` |

---

## 🎓 Kesimpulan

Manipulasi style dengan DOM JavaScript memberikan **kontrol penuh** untuk mengubah tampilan element secara dinamis. Ingat untuk selalu menggunakan format **camelCase** saat mengakses properti style melalui JavaScript! 🚀
